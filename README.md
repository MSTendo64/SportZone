# SportZone - Интернет-магазин спортивных товаров

Django-приложение для онлайн-продажи спортивных товаров.

## 📋 Содержание
- [Технологии](#технологии)
- [Предварительные требования](#предварительные-требования)
- [Установка](#установка)
- [Настройка](#настройка)
- [Запуск](#запуск)
- [Дополнительные команды](#дополнительные-команды)
- [Структура проекта](#структура-проекта)

## 🛠 Технологии
- Python 3.10+
- Django 4.2+
- PostgreSQL (рекомендуется) / SQLite (для разработки)
- Bootstrap 5
- JavaScript (Vanilla)
- Django REST Framework (опционально)

## 📦 Предварительные требования

Перед установкой убедитесь, что у вас установлены:
- Python 3.10 или выше
- pip (менеджер пакетов Python)
- virtualenv (рекомендуется)
- Git

## 🔧 Установка

### 1. Клонирование репозитория
```bash
git clone <url-репозитория>
cd sportzone
```

### 2. Создание виртуального окружения
```bash
# Для Linux/Mac
python3 -m venv venv
source venv/bin/activate

# Для Windows
python -m venv venv
venv\Scripts\activate
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 4. Установка зависимостей для разработки (опционально)
```bash
pip install -r requirements-dev.txt
```

## ⚙️ Настройка

### 1. Создание файла с переменными окружения
Создайте файл `.env` в корневой директории проекта на основе примера:
```bash
cp .env.example .env
```

### 2. Настройка переменных окружения
Отредактируйте файл `.env`:
```env
# Базовые настройки
DEBUG=True
SECRET_KEY=ваш-секретный-ключ
ALLOWED_HOSTS=localhost,127.0.0.1

# Настройки базы данных (для PostgreSQL)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=sportzone_db
DB_USER=ваш_пользователь
DB_PASSWORD=ваш_пароль
DB_HOST=localhost
DB_PORT=5432

# Для SQLite (простая настройка для разработки)
# DB_ENGINE=django.db.backends.sqlite3
# DB_NAME=db.sqlite3

# Настройки почты (опционально)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=ваш-email@gmail.com
EMAIL_HOST_PASSWORD=ваш-пароль

# Настройки медиафайлов
MEDIA_URL=/media/
MEDIA_ROOT=media/
```

### 3. Применение миграций базы данных
```bash
python manage.py makemigrations
python manage.py migrate
```

### 4. Создание суперпользователя
```bash
python manage.py createsuperuser
```
Следуйте инструкциям для создания административной учетной записи.

### 5. Загрузка начальных данных (опционально)
```bash
python manage.py loaddata categories.json
python manage.py loaddata products.json
```

### 6. Сборка статических файлов
```bash
python manage.py collectstatic --no-input
```

## 🚀 Запуск

### Вариант 1: Сервер для разработки
```bash
python manage.py runserver
```
Приложение будет доступно по адресу: http://127.0.0.1:8000

Административная панель: http://127.0.0.1:8000/admin/

### Вариант 2: Запуск с настройкой порта
```bash
python manage.py runserver 0.0.0.0:8080
```

### Вариант 3: Запуск с Gunicorn (для продакшена)
```bash
gunicorn sportzone.wsgi:application --bind 0.0.0.0:8000
```

## 📊 Дополнительные команды

### Создание фикстур
```bash
# Создание фикстур для приложения products
python manage.py dumpdata products --format=json --indent=2 > products/fixtures/products.json
```

### Запуск тестов
```bash
# Все тесты
python manage.py test

# Конкретное приложение
python manage.py test products

# С отчетом о покрытии
coverage run --source='.' manage.py test
coverage report
```

### Проверка кода
```bash
# Проверка стиля кода
flake8 .

# Сортировка импортов
isort .

# Форматирование кода
black .
```

## 📁 Структура проекта
```
sportzone/
├── manage.py
├── requirements.txt
├── .env
├── .gitignore
├── README.md
├── sportzone/              # Основной проект
│   ├── __init__.py
│   ├── settings.py         # Настройки проекта
│   ├── urls.py             # Основные URL-маршруты
│   ├── wsgi.py             # WSGI конфигурация
│   └── asgi.py             # ASGI конфигурация
├── products/               # Приложение товаров
├── cart/                   # Приложение корзины
├── orders/                 # Приложение заказов
├── users/                  # Приложение пользователей
├── payments/               # Приложение оплаты
├── templates/              # HTML шаблоны
├── static/                 # Статические файлы
│   ├── css/
│   ├── js/
│   └── images/
└── media/                  # Медиафайлы (загружаемые)
    ├── products/
    └── categories/
```

## 🔒 Настройки для продакшена

1. Установите `DEBUG=False` в `.env`
2. Обновите `ALLOWED_HOSTS` с вашим доменом
3. Настройте HTTPS (рекомендуется)
4. Используйте PostgreSQL в продакшене
5. Настройте кэширование
6. Настройте сбор статики через Nginx/Apache

## 🐳 Docker (опционально)

Для запуска через Docker:

```bash
# Сборка и запуск
docker-compose up --build

# Только запуск
docker-compose up

# Запуск в фоновом режиме
docker-compose up -d
```
